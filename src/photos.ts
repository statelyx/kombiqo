// Web preview: retain the resized data URI in browser storage.
export function persistPhoto(image?: string): string | undefined { return image; }
export function photoUri(image: string): string { return image; }
export function removePhoto(_image: string): void {}
